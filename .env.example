# Steam API Configuration
STEAM_API_KEY=your_steam_api_key_here

# MongoDB Configuration (Optional - for database features)
MONGODB_URI=mongodb://localhost:27017/case-reaper
# For production (MongoDB Atlas):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/case-reaper

# Application Configuration
NODE_ENV=development
PORT=3000

# Steam OAuth Configuration
STEAM_RETURN_URL=http://localhost:3000/api/auth/steam/callback
STEAM_REALM=http://localhost:3000
# For production:
# STEAM_RETURN_URL=https://case-reaper.vercel.app/api/auth/steam/callback
# STEAM_REALM=https://case-reaper.vercel.app

# Optional: Database Configuration for Production
DB_HOST=localhost
DB_PORT=27017
DB_NAME=case-reaper
DB_USER=
DB_PASSWORD=